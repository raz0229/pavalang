// string.pava
// author: @raz0229

fun charAt(str, i) {
    if (typeof(str) == "String"
        and typeof(i) == "Number" 
        and i < length(str)) {
        return str[i];
    } else {
        return nil;
    }
}

kaddo String;